name: OpenBLAS clang/LLVM OpenMP targeted sweeps

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  arm64-llvm-openmp-sweep:
    name: arm64-llvm-${{ matrix.llvm_major }}-openblas-v0.3.33
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 75

    strategy:
      fail-fast: false
      matrix:
        llvm_major: ["18", "19", "20", "21", "22"]

    steps:
      - name: Check out reproducer repository
        uses: actions/checkout@v4

      - name: Show machine info
        run: |
          set -eux
          uname -a
          uname -m
          lscpu
          nproc

      - name: Install Miniforge
        run: |
          set -eux

          curl -L -o miniforge.sh \
            "https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-aarch64.sh"

          bash miniforge.sh -b -p "$HOME/miniforge3"

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda config --set always_yes yes
          conda config --set channel_priority strict

      - name: Create clang/LLVM OpenMP environment
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"

          conda create -n oblas-clang -c conda-forge \
            git make binutils \
            "clang=${{ matrix.llvm_major }}" \
            "clangxx=${{ matrix.llvm_major }}" \
            "llvm-openmp=${{ matrix.llvm_major }}"

          conda activate oblas-clang

          which clang
          which clang++
          clang --version
          clang++ --version

          echo "CONDA_PREFIX=$CONDA_PREFIX"
          conda list | grep -E 'llvm-openmp|openmp|clang|binutils' || true

          echo "=== libomp files ==="
          find "$CONDA_PREFIX/lib" -maxdepth 1 -name 'libomp*' -print || true

      - name: Build OpenBLAS v0.3.33 libraries with clang/LLVM OpenMP
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"
          export LIBRARY_PATH="$CONDA_PREFIX/lib:${LIBRARY_PATH:-}"

          git clone https://github.com/OpenMathLib/OpenBLAS.git OpenBLAS-tested-ref
          cd OpenBLAS-tested-ref
          git checkout --detach v0.3.33

          echo "=== tested OpenBLAS ref ==="
          git log -1 --oneline

          make -j"$(nproc)" libs \
            CC="$CONDA_PREFIX/bin/clang" \
            USE_OPENMP=1 \
            NO_AFFINITY=1 \
            TARGET=ARMV8 \
            NUM_THREADS=128 \
            NO_LAPACK=1 \
            NOFORTRAN=1

          LIB_CLANG_CLEAN="$(ls "$PWD"/libopenblas*.a | head -1)"
          echo "LIB_CLANG_CLEAN=$LIB_CLANG_CLEAN" >> "$GITHUB_ENV"

          echo "=== OpenBLAS config strings in archive ==="
          strings "$LIB_CLANG_CLEAN" \
            | grep -E 'OpenBLAS|USE_OPENMP|ARMV8' \
            | head -50 || true

          echo "=== OpenMP symbols in OpenBLAS archive ==="
          nm -u "$LIB_CLANG_CLEAN" \
            | grep -E 'GOMP|__kmpc|omp_' \
            | sort -u \
            | head -100 || true

      - name: Compile reproducer
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"
          export LIBRARY_PATH="$CONDA_PREFIX/lib:${LIBRARY_PATH:-}"

          clang -O2 -Wall -Wextra \
            "$GITHUB_WORKSPACE/openblas_dsyr2k_dgemm_minimal_repro.c" \
            "$LIB_CLANG_CLEAN" \
            -L"$CONDA_PREFIX/lib" \
            -Wl,-rpath,"$CONDA_PREFIX/lib" \
            -lomp -lpthread -lm -ldl \
            -o repro_clang_llvm_omp

      - name: Check linkage and symbols
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"

          echo "=== ldd ==="
          ldd ./repro_clang_llvm_omp || true

          echo "=== OpenMP-related undefined symbols in final executable ==="
          nm -u ./repro_clang_llvm_omp \
            | grep -E 'GOMP|__kmpc|omp_' \
            | sort -u || true

      - name: Run positive controls
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"

          echo "=== mutex control ==="
          OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 1 32 32 64 64 128 1000

          echo "=== single-thread control ==="
          OMP_NUM_THREADS=1 ./repro_clang_llvm_omp 0 32 32 64 64 128 1000

          echo "=== DSYR2K below threshold control ==="
          OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 0 31 32 64 64 128 1000

          echo "=== DGEMM below threshold control ==="
          OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 0 32 32 64 64 127 1000

      - name: Run threaded no-mutex correctness stress test
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"

          for i in $(seq 1 10); do
            echo "=== attempt $i: default geometry, calls=1000 ==="
            OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 0 32 32 64 64 128 1000
          done

  arm64-openblas-ref-sweep:
    name: arm64-openblas-${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 75

    strategy:
      fail-fast: false
      matrix:
        include:
          - label: v0.3.30
            openblas_ref: v0.3.30

          - label: before-issue5414
            openblas_ref: "4cd575c20^1"

          - label: issue5414
            openblas_ref: "4cd575c20"

          - label: v0.3.31
            openblas_ref: v0.3.31

          - label: v0.3.32
            openblas_ref: v0.3.32

          - label: v0.3.33
            openblas_ref: v0.3.33

          - label: develop
            openblas_ref: develop

    steps:
      - name: Check out reproducer repository
        uses: actions/checkout@v4

      - name: Show machine info
        run: |
          set -eux
          uname -a
          uname -m
          lscpu
          nproc

      - name: Install Miniforge
        run: |
          set -eux

          curl -L -o miniforge.sh \
            "https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-aarch64.sh"

          bash miniforge.sh -b -p "$HOME/miniforge3"

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda config --set always_yes yes
          conda config --set channel_priority strict

      - name: Create clang/LLVM OpenMP environment
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"

          conda create -n oblas-clang -c conda-forge \
            git make binutils \
            "clang=22" \
            "clangxx=22" \
            "llvm-openmp=22"

          conda activate oblas-clang

          which clang
          which clang++
          clang --version
          clang++ --version

          echo "CONDA_PREFIX=$CONDA_PREFIX"
          conda list | grep -E 'llvm-openmp|openmp|clang|binutils' || true

          echo "=== libomp files ==="
          find "$CONDA_PREFIX/lib" -maxdepth 1 -name 'libomp*' -print || true

      - name: Build OpenBLAS ref
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"
          export LIBRARY_PATH="$CONDA_PREFIX/lib:${LIBRARY_PATH:-}"

          OPENBLAS_REF="${{ matrix.openblas_ref }}"

          git clone https://github.com/OpenMathLib/OpenBLAS.git OpenBLAS-tested-ref
          cd OpenBLAS-tested-ref

          if [ "$OPENBLAS_REF" = "develop" ]; then
            git checkout --detach origin/develop
          else
            git checkout --detach "$OPENBLAS_REF"
          fi

          echo "=== tested OpenBLAS ref ==="
          git log -1 --oneline
          git status --short

          make -j"$(nproc)" libs \
            CC="$CONDA_PREFIX/bin/clang" \
            USE_OPENMP=1 \
            NO_AFFINITY=1 \
            TARGET=ARMV8 \
            NUM_THREADS=128 \
            NO_LAPACK=1 \
            NOFORTRAN=1

          LIB_CLANG_CLEAN="$(ls "$PWD"/libopenblas*.a | head -1)"
          echo "LIB_CLANG_CLEAN=$LIB_CLANG_CLEAN" >> "$GITHUB_ENV"

          echo "=== OpenBLAS config strings in archive ==="
          strings "$LIB_CLANG_CLEAN" \
            | grep -E 'OpenBLAS|USE_OPENMP|ARMV8' \
            | head -50 || true

          echo "=== OpenMP symbols in OpenBLAS archive ==="
          nm -u "$LIB_CLANG_CLEAN" \
            | grep -E 'GOMP|__kmpc|omp_' \
            | sort -u \
            | head -100 || true

      - name: Compile reproducer
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"
          export LIBRARY_PATH="$CONDA_PREFIX/lib:${LIBRARY_PATH:-}"

          clang -O2 -Wall -Wextra \
            "$GITHUB_WORKSPACE/openblas_dsyr2k_dgemm_minimal_repro.c" \
            "$LIB_CLANG_CLEAN" \
            -L"$CONDA_PREFIX/lib" \
            -Wl,-rpath,"$CONDA_PREFIX/lib" \
            -lomp -lpthread -lm -ldl \
            -o repro_clang_llvm_omp

      - name: Check linkage and symbols
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"

          echo "=== ldd ==="
          ldd ./repro_clang_llvm_omp || true

          echo "=== OpenMP-related undefined symbols in final executable ==="
          nm -u ./repro_clang_llvm_omp \
            | grep -E 'GOMP|__kmpc|omp_' \
            | sort -u || true

      - name: Run positive controls
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"

          echo "=== mutex control ==="
          OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 1 32 32 64 64 128 1000

          echo "=== single-thread control ==="
          OMP_NUM_THREADS=1 ./repro_clang_llvm_omp 0 32 32 64 64 128 1000

          echo "=== DSYR2K below threshold control ==="
          OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 0 31 32 64 64 128 1000

          echo "=== DGEMM below threshold control ==="
          OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 0 32 32 64 64 127 1000

      - name: Run threaded no-mutex correctness stress test
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"

          for i in $(seq 1 10); do
            echo "=== attempt $i: default geometry, calls=1000 ==="
            OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 0 32 32 64 64 128 1000
          done

  x86_64-reference:
    name: x86_64-llvm-22-openblas-v0.3.33
    runs-on: ubuntu-24.04
    timeout-minutes: 75

    steps:
      - name: Check out reproducer repository
        uses: actions/checkout@v4

      - name: Show machine info
        run: |
          set -eux
          uname -a
          uname -m
          lscpu
          nproc

      - name: Install Miniforge
        run: |
          set -eux

          curl -L -o miniforge.sh \
            "https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-x86_64.sh"

          bash miniforge.sh -b -p "$HOME/miniforge3"

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda config --set always_yes yes
          conda config --set channel_priority strict

      - name: Create clang/LLVM OpenMP environment
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"

          conda create -n oblas-clang -c conda-forge \
            git make binutils \
            "clang=22" \
            "clangxx=22" \
            "llvm-openmp=22"

          conda activate oblas-clang

          which clang
          which clang++
          clang --version
          clang++ --version

          echo "CONDA_PREFIX=$CONDA_PREFIX"
          conda list | grep -E 'llvm-openmp|openmp|clang|binutils' || true

          echo "=== libomp files ==="
          find "$CONDA_PREFIX/lib" -maxdepth 1 -name 'libomp*' -print || true

      - name: Build OpenBLAS v0.3.33 libraries with clang/LLVM OpenMP
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"
          export LIBRARY_PATH="$CONDA_PREFIX/lib:${LIBRARY_PATH:-}"

          git clone https://github.com/OpenMathLib/OpenBLAS.git OpenBLAS-tested-ref
          cd OpenBLAS-tested-ref
          git checkout --detach v0.3.33

          echo "=== tested OpenBLAS ref ==="
          git log -1 --oneline

          make -j"$(nproc)" libs \
            CC="$CONDA_PREFIX/bin/clang" \
            USE_OPENMP=1 \
            NO_AFFINITY=1 \
            NUM_THREADS=128 \
            NO_LAPACK=1 \
            NOFORTRAN=1

          LIB_CLANG_CLEAN="$(ls "$PWD"/libopenblas*.a | head -1)"
          echo "LIB_CLANG_CLEAN=$LIB_CLANG_CLEAN" >> "$GITHUB_ENV"

          echo "=== OpenBLAS config strings in archive ==="
          strings "$LIB_CLANG_CLEAN" \
            | grep -E 'OpenBLAS|USE_OPENMP' \
            | head -50 || true

          echo "=== OpenMP symbols in OpenBLAS archive ==="
          nm -u "$LIB_CLANG_CLEAN" \
            | grep -E 'GOMP|__kmpc|omp_' \
            | sort -u \
            | head -100 || true

      - name: Compile reproducer
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"
          export LIBRARY_PATH="$CONDA_PREFIX/lib:${LIBRARY_PATH:-}"

          clang -O2 -Wall -Wextra \
            "$GITHUB_WORKSPACE/openblas_dsyr2k_dgemm_minimal_repro.c" \
            "$LIB_CLANG_CLEAN" \
            -L"$CONDA_PREFIX/lib" \
            -Wl,-rpath,"$CONDA_PREFIX/lib" \
            -lomp -lpthread -lm -ldl \
            -o repro_clang_llvm_omp

      - name: Check linkage and symbols
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"

          echo "=== ldd ==="
          ldd ./repro_clang_llvm_omp || true

          echo "=== OpenMP-related undefined symbols in final executable ==="
          nm -u ./repro_clang_llvm_omp \
            | grep -E 'GOMP|__kmpc|omp_' \
            | sort -u || true

      - name: Run positive controls
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"

          echo "=== mutex control ==="
          OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 1 32 32 64 64 128 1000

          echo "=== single-thread control ==="
          OMP_NUM_THREADS=1 ./repro_clang_llvm_omp 0 32 32 64 64 128 1000

          echo "=== DSYR2K below threshold control ==="
          OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 0 31 32 64 64 128 1000

          echo "=== DGEMM below threshold control ==="
          OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 0 32 32 64 64 127 1000

      - name: Run threaded no-mutex correctness stress test
        run: |
          set -eux

          source "$HOME/miniforge3/etc/profile.d/conda.sh"
          conda activate oblas-clang

          export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:${LD_LIBRARY_PATH:-}"

          for i in $(seq 1 10); do
            echo "=== attempt $i: default geometry, calls=1000 ==="
            OMP_NUM_THREADS=8 ./repro_clang_llvm_omp 0 32 32 64 64 128 1000
          done
